const target=new Date('2026-10-03T00:00:00+05:30').getTime();
function tick(){const delta=Math.max(0,target-Date.now());const values=[Math.floor(delta/86400000),Math.floor(delta/3600000)%24,Math.floor(delta/60000)%60,Math.floor(delta/1000)%60];['days','hours','mins','secs'].forEach((id,i)=>document.getElementById(id).textContent=String(values[i]).padStart(2,'0'));if(delta===0){document.getElementById('count-note').textContent=Date.now()<target+86400000?'இன்று திருவிழா! அனைவரும் வருக!':'ஹரே கிருஷ்ண!';}}tick();setInterval(tick,1000);
let audio;document.getElementById('bell').addEventListener('click',async()=>{try{audio??=new(window.AudioContext||window.webkitAudioContext)();await audio.resume();const now=audio.currentTime;[523.25,1046.5,1568,2180,2877].forEach((freq,i)=>{const osc=audio.createOscillator(),gain=audio.createGain();osc.type='sine';osc.frequency.value=freq;gain.gain.setValueAtTime(0,now);gain.gain.linearRampToValueAtTime(.16/(i+1),now+.012);gain.gain.exponentialRampToValueAtTime(.0001,now+3.7-i*.3);osc.connect(gain);gain.connect(audio.destination);osc.start(now);osc.stop(now+4);});document.getElementById('bell-status').textContent='கோயில் மணி ஒலிக்கிறது';}catch{document.getElementById('bell-status').textContent='இந்த உலாவியில் ஒலி கிடைக்கவில்லை';}});
